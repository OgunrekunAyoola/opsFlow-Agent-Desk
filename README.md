# OpsFlow Agent Desk (v1)

OpsFlow Agent Desk is a multi-tenant support platform that leverages AI to triage tickets and draft replies for agency teams.

## Tech Stack

- **Backend**: Node.js, Express, TypeScript, MongoDB (Mongoose)
- **Frontend**: React, TypeScript, Vite
- **AI**: OpenAI (or compatible LLM)

## Getting Started

### Prerequisites

- Node.js (v18+)
- MongoDB
- npm or yarn

### Setup

1.  Clone the repository.
2.  Install dependencies:
    ```bash
    cd backend && npm install
    cd ../frontend && npm install
    ```
3.  Set up environment variables:
    - Copy `.env.example` to `backend/.env` and configure.
4.  Run the development servers:
    - Backend: `cd backend && npm run dev`
    - Frontend: `cd frontend && npm run dev`

## Documentation

See `docs/` folder for detailed specs and tasks.
